# Simple test for cumulative operations
# Updated to Unified Syntax v2.0

# Load data (unified syntax with auto-detection)
load "data/sales_data.csv" as sales

# Test cumulative sum
cumsum sales column quantity as cumulative_quantity

# Display result
describe cumulative_quantity
